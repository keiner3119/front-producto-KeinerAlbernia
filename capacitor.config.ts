import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'front-producto-keinerAlbernia',
  webDir: 'www'
};

export default config;
